<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinTrivia"
        tools:targetApi="31">
        <activity
            android:name=".presentation.game.GameRoundActivity"
            android:exported="false"
            android:label="@string/title_activity_game_round"
            android:theme="@style/Theme.KotlinTrivia" />
        <activity
            android:name=".presentation.home.roundscores.RoundScoresActivity"
            android:exported="false"
            android:label="@string/title_activity_round_scores"
            android:theme="@style/Theme.KotlinTrivia" />
        <activity
            android:name=".presentation.home.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.KotlinTrivia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>